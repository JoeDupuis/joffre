<h1>New Game</h1>

<%= form_with model: @game do |form| %>
  <%= form_errors(@game) %>

  <div>
    <%= form.label :name %>
    <%= form.text_field :name, required: true, autofocus: true, placeholder: "Enter game name" %>
  </div>

  <div>
    <%= form.submit "Create Game" %>
  </div>
<% end %>

<%= link_to "Back to Dashboard", root_path %>
